<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
  'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>

<mapper namespace='py.pol.una.ii.pw.mapper.MemberMapper'>

  <resultMap type="py.pol.una.ii.pw.model.Member" id="MemberResult">
  	 <id property='id' column='id'/>
    <result property='name' column='name'/>
    <result property='email' column='email'/>
    <result property='phoneNumber' column='phoneNumber'/>
  </resultMap>

  <select id="getAllMembers" parameterType="int" resultType="py.pol.una.ii.pw.model.Member">
   SELECT * FROM MEMBER WHERE id = #{id}
  </select>
  
  <select id="selectMemberById" parameterType="int" resultType="py.pol.una.ii.pw.model.Member">
  	select * from member where id = #{id}
  </select>

   <insert id='insertMember' parameterType='py.pol.una.ii.pw.model.Member' useGeneratedKeys='true' keyProperty='id'>
   INSERT INTO MEMBER(name,email, phoneNumber)
    VALUES(#{name}, #{email}, #{phoneNumber})
  </insert>
<!--
  <update id='updateUser' parameterType='User'>
    UPDATE USER 
    SET
     PASSWORD= #{password},
     FIRST_NAME = #{firstName},
     LAST_NAME = #{lastName}
    WHERE USER_ID = #{userId}
  </update>

  <delete id='deleteUser' parameterType='int'>
    DELETE FROM USER WHERE USER_ID = #{userId}
  </delete>
 -->
</mapper>